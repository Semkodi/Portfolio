export default {
    plugins: {
        '@tailwindcss/postcss': {}, // Tailwind PostCSS Plugin für v4 // Erklärung
        autoprefixer: {}, // Autoprefixer für Browserkompatibilität // Erklärung
    },
}
